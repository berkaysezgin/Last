import { FilterDiscontinuedPipe } from './filter-discontinued.pipe';

describe('FilterDiscontinuedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDiscontinuedPipe();
    expect(pipe).toBeTruthy();
  });
});
